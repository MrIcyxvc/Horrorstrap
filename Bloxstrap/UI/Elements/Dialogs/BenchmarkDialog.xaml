<base:WpfUiWindow x:Class="Bloxstrap.UI.Elements.Dialogs.BenchmarkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:Bloxstrap.UI.Elements.Dialogs"
        xmlns:base="clr-namespace:Bloxstrap.UI.Elements.Base"
        mc:Ignorable="d"
        Title="Benchmark Complete!"
        MinHeight="0"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        Background="{DynamicResource ApplicationBackground}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Grid.ColumnSpan="2" Padding="8" Title="Benchmark Complete!" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" KeyboardNavigation.TabNavigation="None" />

        <StackPanel Grid.Row="1" Margin="24,16,24,8">
            <TextBlock FontSize="14" Margin="0,0,0,12" Foreground="{DynamicResource TextFillColorPrimaryBrush}">
                <Run Text="Performance Score: " />
                <Run x:Name="ScoreRun" FontWeight="SemiBold" />
                <Run Text="/100" />
            </TextBlock>
            <TextBlock FontSize="14" Margin="0,0,0,12" Foreground="{DynamicResource TextFillColorPrimaryBrush}">
                <Run Text="System Tier: " />
                <Run x:Name="TierRun" FontWeight="SemiBold" />
            </TextBlock>
            <TextBlock FontSize="14" Margin="0,0,0,12" Foreground="{DynamicResource TextFillColorPrimaryBrush}">
                <Run Text="Recommended Preset: " />
                <Run x:Name="PresetRun" FontWeight="SemiBold" />
            </TextBlock>
            <TextBlock FontSize="13" Text="Would you like to apply the recommended preset?" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,8,24,24">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="CancelButton_Click" />
            <Button Content="Apply" Width="90" Click="ApplyButton_Click" />
        </StackPanel>
    </Grid>
</base:WpfUiWindow>
