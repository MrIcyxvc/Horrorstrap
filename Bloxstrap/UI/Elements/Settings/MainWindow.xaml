<base:WpfUiWindow x:Class="Bloxstrap.UI.Elements.Settings.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:pages="clr-namespace:Bloxstrap.UI.Elements.Settings.Pages"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:base="clr-namespace:Bloxstrap.UI.Elements.Base"
             xmlns:resources="clr-namespace:Bloxstrap.Resources"
             mc:Ignorable="d"
             Title="{x:Static resources:Strings.Menu_Title}"
             MinWidth="1280"
             Width="1280"
             Height="720"
             Background="{DynamicResource ApplicationBackground}"
             ExtendsContentIntoTitleBar="True"
             WindowBackdropType="Mica"   
             WindowStartupLocation="CenterScreen"
             Closing="WpfUiWindow_Closing"
             Closed="WpfUiWindow_Closed">

    <Grid Background="{DynamicResource ApplicationBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="87*"/>
            <ColumnDefinition Width="163*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:Snackbar
            x:Name="AlreadyRunningSnackbar"
            Title="{x:Static resources:Strings.Menu_AlreadyRunning_Title}"
            Message="{x:Static resources:Strings.Menu_AlreadyRunning_Caption}"
            Icon="Info20" Timeout="10000" Margin="200,0,200,20" Grid.RowSpan="2" Panel.ZIndex="10" Grid.ColumnSpan="2" Grid.Row="1" />

        <ui:Snackbar
            x:Name="SettingsSavedSnackbar"
            Title="{x:Static resources:Strings.Menu_SettingsSaved_Title}"
            Message="{x:Static resources:Strings.Menu_SettingsSaved_Message}"
            Appearance="Success"
            Background="Black"
            Foreground="White"
            MessageForeground="White"
            IconForeground="White"
            Icon="CheckmarkCircle32"
            Timeout="3000"
            Margin="200,0,200,20"
            Grid.RowSpan="2"
            Panel.ZIndex="9"
            Grid.ColumnSpan="2"
            Grid.Row="1" />

        <ui:TitleBar Padding="8" x:Name="RootTitleBar" ForceShutdown="False" MinimizeToTray="False" UseSnapLayout="True" 
                     Title="{x:Static resources:Strings.Menu_Title}" Icon="pack://application:,,,/Horrorstrap.ico" Grid.ColumnSpan="2" VerticalAlignment="Center" />

        <Grid x:Name="RootGrid" Grid.Row="1" Margin="12,12,0,0" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:AutoSuggestBox
                x:Name="SearchBox"
                Grid.Row="0"
                Grid.Column="0"
                Margin="0,0,12,8"
                PlaceholderText="Search settings"
                Icon="Search24"
                SuggestionChosen="SearchBox_SuggestionChosen" />

            <ui:NavigationFluent 
                x:Name="RootNavigation" 
                Grid.Row="1" 
                Grid.Column="0" 
                Margin="0,0,12,0" 
                Frame="{Binding ElementName=RootFrame}" 
                SelectedPageIndex="0">

                <ui:NavigationFluent.Items>
                    <ui:NavigationItem Content="{x:Static resources:Strings.Menu_Integrations_Title}" FontWeight="Medium" PageType="{x:Type pages:IntegrationsPage}" Icon="Add28" Tag="integrations" />
                    <ui:NavigationItem Content="{x:Static resources:Strings.Menu_Behaviour_Title}" FontWeight="Medium" PageType="{x:Type pages:BehaviourPage}" Icon="PlaySettings20" Tag="bootstrapper" />
                    <ui:NavigationItem Content="{x:Static resources:Strings.Common_Deployment}" FontWeight="Medium" PageType="{x:Type pages:ChannelPage}" Icon="CloudArchive20" Tag="channel" />
                    <ui:NavigationItem Content="{x:Static resources:Strings.Menu_Mods_Title}" FontWeight="Medium" PageType="{x:Type pages:ModsPage}" Icon="WrenchScrewdriver20" Tag="mods" />
                    <ui:NavigationItem x:Name="fastflags" Content="{x:Static resources:Strings.Menu_FastFlags_Title}" FontWeight="Medium" PageType="{x:Type pages:FastFlagsPage}" Icon="Flag24" Tag="fastflags" />
                    <ui:NavigationItem x:Name="gbs" Content="{x:Static resources:Strings.Common_GlobalSettings}" FontWeight="Medium" PageType="{x:Type pages:GlobalSettingsPage}" Icon="Settings20" Tag="gbseditor"/>
                    <ui:NavigationItem Content="{x:Static resources:Strings.Menu_Appearance_Title}" FontWeight="Medium" PageType="{x:Type pages:AppearancePage}" Icon="PaintBrush24" Tag="appearance" />
                    <ui:NavigationItem Content="{x:Static resources:Strings.Common_Shortcuts}" FontWeight="Medium" PageType="{x:Type pages:ShortcutsPage}" Icon="Apps28" Tag="shortcuts" />
                    <ui:NavigationItem Content="Windows Tweaks" FontWeight="Medium" PageType="{x:Type pages:WindowsTweaksPage}" Icon="Desktop24" Tag="windowstweaks" />

                    <ui:NavigationItem Content="{x:Static resources:Strings.Menu_FastFlagEditor_Title}" PageType="{x:Type pages:FastFlagEditorPage}" Tag="fastflageditor" Visibility="Collapsed" />
                    <ui:NavigationItem Content="IXP Editor" PageType="{x:Type pages:IxpEditorPage}" Tag="ixpeditor" Visibility="Collapsed" />
                </ui:NavigationFluent.Items>

                <ui:NavigationFluent.Footer>
                    <ui:NavigationItem Content="{x:Static resources:Strings.Menu_About_Title}" FontWeight="Medium" Icon="QuestionCircle24" Margin="0,0,0,12" Command="{Binding OpenAboutCommand}" Click="NavigationItem_Click" />
                </ui:NavigationFluent.Footer>
            </ui:NavigationFluent>

            <Grid Grid.Row="0" Grid.RowSpan="2" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ui:InfoBar x:Name="AlertBar" Grid.Row="0" Margin="0,0,12,0" Padding="8" Visibility="Collapsed" IsOpen="True" Message="" Severity="Warning" IsClosable="False" />
                <ui:Breadcrumb Grid.Row="1" Margin="0,0,0,5" Navigation="{Binding ElementName=RootNavigation}" />
                <Frame x:Name="RootFrame" Grid.Row="2" />
            </Grid>
        </Grid>

        <Border Grid.Row="2"
                VerticalAlignment="Center"
                Height="56"
                Background="{ui:ThemeResource ControlFillColorDefaultBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                CornerRadius="12,12,0,0"
                Padding="20,0" Grid.ColumnSpan="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:ToggleSwitch IsChecked="{Binding TestModeEnabled, Mode=TwoWay}" />
                    <TextBlock Padding="10,0,0,0" Text="{x:Static resources:Strings.Menu_TestMode}" FontWeight="Medium" VerticalAlignment="Center" FontSize="13" />
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:Button Margin="0,0,8,0" Padding="12,6" Content="{x:Static resources:Strings.Menu_SaveAndLaunch}" Background="Black" Foreground="White" BorderBrush="Black" FontWeight="SemiBold" Command="{Binding SaveAndLaunchSettingsCommand}" />
                    <ui:Button Margin="0,0,8,0" Padding="12,6" Content="{x:Static resources:Strings.Menu_Save}" Background="Black" Foreground="White" BorderBrush="Black" FontWeight="SemiBold" Command="{Binding SaveSettingsCommand}" />
                    <ui:Button Padding="12,6" Content="{x:Static resources:Strings.Common_Close}" Background="Black" Foreground="White" BorderBrush="Black" FontWeight="SemiBold" Command="{Binding CloseWindowCommand}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</base:WpfUiWindow>