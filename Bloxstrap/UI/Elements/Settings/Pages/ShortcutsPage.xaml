<ui:UiPage x:Class="Bloxstrap.UI.Elements.Settings.Pages.ShortcutsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:Bloxstrap.UI.Elements.Settings.Pages"
      xmlns:controls="clr-namespace:Bloxstrap.UI.Elements.Controls"
      xmlns:dmodels="clr-namespace:Bloxstrap.UI.ViewModels.Settings"
      xmlns:resources="clr-namespace:Bloxstrap.Resources"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance dmodels:ShortcutsViewModel, IsDesignTimeCreatable=True}"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="ShortcutsPage"
      Scrollable="True">

    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="{x:Static resources:Strings.Menu_Shortcuts_Description}" Style="{StaticResource SectionDescription}" />

        <TextBlock Text="Miscellaneous" Style="{StaticResource SectionHeader}" Margin="0,16,0,0" />
        <ui:CardExpander Margin="0,8,0,0" IsExpanded="False">
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock FontSize="14" Text="Game Shortcuts" />
                        <TextBlock Margin="0,2,0,0" FontSize="12" Text="This allows you to create shortcuts which let you join specific games quickly." Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    </StackPanel>
                </Grid>
            </ui:CardExpander.Header>

            <StackPanel Margin="0,8,0,0">
                <Border Width="96" Height="96" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="16" HorizontalAlignment="Left" Margin="0,0,0,16">
                    <ui:SymbolIcon Symbol="Games24" FontSize="48" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </Border>

                <TextBlock Text="Search using game name" FontSize="14" Margin="0,0,0,4" />
                <ComboBox x:Name="GameSearchBox" IsEditable="True" Width="Auto" Padding="10,5,10,5" Margin="0,0,0,12" />

                <ui:Button Icon="DocumentCopy24" Content="Create Game Shortcut" Margin="0,0,0,4" Click="CreateGameShortcut_Click" />
                <TextBlock Text="Game ID required" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            </StackPanel>
        </ui:CardExpander>

        <controls:OptionControl
            Header="{x:Static resources:Strings.Menu_Shortcuts_ExtractIcons_Title}"
            Description="{x:Static resources:Strings.Menu_Shortcuts_ExtractIcons_Description}"
            HelpLink="https://bloxstraplabs.com/wiki/info/using-roblox-icons-for-shortcuts/"
            Icon="ImageMultiple24"
            Margin="0,0,0,8">
            <ui:ToggleSwitch IsChecked="{Binding ExtractIconsTask.NewState, Mode=TwoWay}" />
        </controls:OptionControl>

        <TextBlock Text="{x:Static resources:Strings.Menu_Shortcuts_General_Title}" Style="{StaticResource SectionHeader}" />
        <TextBlock Text="{x:Static resources:Strings.Menu_Shortcuts_General_Description}" Style="{StaticResource SectionDescription}" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <controls:OptionControl Grid.Column="0" Margin="0,0,4,0" Header="{x:Static resources:Strings.Common_Shortcuts_Desktop}" Icon="Desktop24">
                <ui:ToggleSwitch IsChecked="{Binding DesktopIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>

            <controls:OptionControl Grid.Column="1" Margin="4,0,0,0" Header="{x:Static resources:Strings.Common_Shortcuts_StartMenu}" Icon="AppsList24">
                <ui:ToggleSwitch IsChecked="{Binding StartMenuIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>
        </Grid>

        <TextBlock Text="{x:Static resources:Strings.Menu_Shortcuts_Function_Title}" Style="{StaticResource SectionHeader}" />
        <TextBlock Text="{x:Static resources:Strings.Menu_Shortcuts_Function_Description}" Style="{StaticResource SectionDescription}" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <!-- https://stackoverflow.com/a/51967350 -->
                <ColumnDefinition>
                    <ColumnDefinition.Style>
                        <Style TargetType="ColumnDefinition">
                        <Setter Property="Width" Value="*" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsStudioOptionVisible}" Value="False">
                                <Setter Property="Width" Value="0" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ColumnDefinition.Style>
                </ColumnDefinition>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <controls:OptionControl Grid.Column="0" Margin="0,0,2,0" Header="{x:Static resources:Strings.LaunchMenu_LaunchRoblox}" Icon="Play24">
                <ui:ToggleSwitch IsChecked="{Binding PlayerIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>

            <controls:OptionControl Grid.Column="1" Margin="2,0,2,0" Header="{x:Static resources:Strings.LaunchMenu_LaunchRobloxStudio}" Icon="Code24">
                <ui:ToggleSwitch IsChecked="{Binding StudioIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>

            <controls:OptionControl Grid.Column="2" Margin="2,0,0,0" Header="{x:Static resources:Strings.Menu_Title}" Icon="Settings24">
                <ui:ToggleSwitch IsChecked="{Binding SettingsIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>
        </Grid>
    </StackPanel>
</ui:UiPage>
